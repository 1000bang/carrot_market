import 'package:flutter/material.dart';

class NeigborhoodScreen extends StatelessWidget {
  const NeigborhoodScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
